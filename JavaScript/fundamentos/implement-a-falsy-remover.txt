** start of script.js **

function bouncer(array) {
  let nuevoArray = []; 
  for (let i = 0; i < array.length; i++) {
    if (array[i]) { 
      nuevoArray.push(array[i]);
    }
  }
  return nuevoArray;
}

console.log(bouncer([]));

** end of script.js **

