** start of script.js **

function getIndexToIns (arr, num) {
  const index = arr.sort((a, b) => a - b).findIndex(x => x >= num); 
  return index === -1 ? arr.length : index;
}

console.log(getIndexToIns([3, 10, 5], 11))

** end of script.js **

